"use client";

import FlowerDeck from "./FlowerDeck";

// Demo showcase in a social-media-style 9:16 frame — the standard deck, on the
// default generic flower. Subsequent pages reuse FlowerDeck with a different
// flower (see DahliaCanvas).
export default function DemoCanvas() {
  return <FlowerDeck />;
}
