# Crimson Rose FlowerSpec

- `referenceSummary`: A deep red rose photographed from slightly above. Broad outer petals sag outward while smaller inner petals stand upright and curl into a tight spiral.
- `structureFamily`: `compact-rosette`
- `silhouette`: `domed`
- `petalProfile`: `rounded`
- `density`: `dense`
- `openness`: `half-open`
- `centerTreatment`: `soft-core`
- `palette`: dark burgundy rim, crimson petals, warm coral-red inner highlights, near-black background.
- `motion`: `blooming`

## Fidelity

- Layered rounded petals: `direct`
- Tight, upright inner spiral: `approximate`
- Broad, slightly drooping outer rim: `direct`
- Crimson-to-coral colour relationship: `direct`
- Water droplets and fine petal texture: `unsupported`
- Serrated leaves and thorned stem: `approximate`

## Intentional approximation

The scene uses one shared petal profile, so it suggests the reference's different inner and outer petal varieties with scale, opening state, curl, and colour rather than modeling each layer separately.
